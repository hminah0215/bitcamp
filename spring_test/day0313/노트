$(a).html();
$(a).text();

$(a).val();		<--- a가 입력양식일때 사용해요.(input 등)


$(a).val("안녕");	<--값을 설정하기
$(a).val();			<--값을 읽어오기


=============================================================================================================

<< 이벤트 연결 >> 

$("선택자").bind("이벤트이름", 함수)


$("tr").bind("click", 함수);
$("tr").click(함수);
	==> 처음부터 있는 tr에만 이벤트가 적용되어요.

미래에 만들어진 tr에도 이벤트를 적용하고 싶어요. 

$("tr").live("click", 함수);
	==> 처음부터 만들어진것, 미래에 만들어진 tr에도 이벤트가 적용됩니다.
		but, 옛날버전... 이며 현재는 on함수를 사용해요.
	
---------------------------------------------------------------------------
<< 현재 버전에서 미래에 생성된 노드에도 이벤트 추가 >>

$(document).on("이벤트이름","선택자",함수);

$(document).on("","click",function(){});	

	
	
	
	










